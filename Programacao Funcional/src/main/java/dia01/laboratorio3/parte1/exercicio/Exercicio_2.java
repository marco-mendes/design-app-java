package dia01.laboratorio3.parte1.exercicio;

import java.util.Arrays;
import java.util.List;
import java.util.function.Function;

/*
  Com base no código abaixo altere a função verificarAlunoAprovado para que a mesma receba uma função composta baseada na função calcularMedia
  e retorne true caso a média seja maior que 6, caso contrário retorne false.
* */

public class Exercicio_2 {

    public static void main(String[] args) {

        /* Remova os comentários do método main para começar!

        Function<List<Integer>, Integer> calcularMedia = (lista) -> {
            int soma = 0;
            for(int valor : lista){
                soma += valor;
            }
            int media = soma / lista.size();
            System.out.println(String.format("Media: %d", media));
            return media;
        };
        Function<List<Integer>, Boolean> verificarAlunoAprovado = ??????;

        List<Integer> notas = Arrays.asList(10, 5, 8, 1, 9);
        System.out.println(String.format("Aluno aprovado? %b", verificarAlunoAprovado.apply(notas)));

        */

    }

}
